pymobiledevice3==7.6.0
pyimg4<0.8.4  # Avoid pylzss/lzfse C extensions (no wheels for Python >=3.14)
PySide6-Essentials; sys_platform != 'linux'
PySide6; sys_platform == 'linux'
PyInstaller
ffmpeg
ffmpeg-python
opencv-python
pyperclip
pyuac; sys_platform == 'windows'  # Optional — only needed for UAC re-launch on Windows